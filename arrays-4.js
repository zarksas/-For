const arr =  ['JavaScript', 'Python', 'PHP', 'Java', 'C++', 'Go', 'C#', 'Fortan', 'Scratch', 'C'];

for (let i = 0; i < arr.length; i++) {
    if (arr[i].length >= 3) {
        console.log(arr[i])
    }
};
