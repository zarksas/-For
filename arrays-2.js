const arr = ['contra', 'hitman', 'callOfduty', 'GTA5', 'battlefield', 'medalOfhonor', 'manhunt', 'stronghold', 'strongholdCrusader', 'unreal'];

for (let i = 0; i < arr.length; i++) {
    console.log(arr[i])
}